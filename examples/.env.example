# Redis 설정
REDIS_URL=redis://localhost:6379/0
# 32바이트 암호화 키 생성 방법:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
REDIS_ENCRYPTION_KEY=your-32-byte-encryption-key-here

# OpenAI API 설정
OPENAI_API_KEY=your-openai-api-key-here

# 로깅 설정
LOG_LEVEL=INFO

# OCR 설정 (선택사항)
TESSERACT_CMD=/usr/local/bin/tesseract  # Tesseract 실행 파일 경로
TESSERACT_LANG=kor+eng                  # OCR 언어 설정
